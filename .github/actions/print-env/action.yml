name: 'Print Environment Variables'
description: 'A GitHub Action to print environment variables'
inputs:
  envs:
    description: 'List of environment variables to print'
    required: false
    default: 'ALL'

runs:
  using: 'composite'
  steps:
    - run: |
        if [ "${{ inputs.envs }}" = "ALL" ]; then
          env
        else
          for var in ${{ inputs.envs }}; do
            echo "$var=${!var}"
          done
        fi
      shell: bash
